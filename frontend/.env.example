# Copy to frontend/.env.local and replace every placeholder before local development.
# This URL is server-only BFF configuration. Do not add secrets or NEXT_PUBLIC_* values.
CONTROL_PLANE_API_BASE_URL=<http-url-to-go-api-v1>
