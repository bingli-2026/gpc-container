#import "../template/project-kit.typ": *

#show: project-document.with(
  title: "异构算力容器教学服务平台总设计方案",
  subtitle: "长期架构基线、领域边界与演进治理",
  doc-type: "MASTER ARCHITECTURE DESIGN",
  version: "V1.0",
  date: "2026-07-23",
  organization: "项目组",
  owner: "冶秉礼",
  status: "内部评审稿",
  toc: true,
)

= 执行摘要

本文件是异构算力容器教学服务平台的长期总设计基线。平台面向昇腾算子及后续异构算力教学，将受控计算资源、持久化学习环境、班级治理、教学过程证据与运营治理组合为统一服务。

一期以单节点环境中的 CPU/NPU 工作区闭环为重点；后续能力必须在不破坏已发布的身份、班级隔离、工作区状态、操作追踪、数据留存和审计语义的前提下演进。

#metric-grid((
  metric-card("平台定位", "教学算力控制面", detail: "不只是资源申请门户"),
  metric-card("最小治理单元", "班级", detail: "权限、资源、数据和证据边界"),
  metric-card("基础资源", "持久工作区", detail: "CPU、NPU 及后续异构能力"),
))

#callout(
  "设计定位",
  [总设计描述长期不变量和各阶段之间的关系；具体交付范围、实现细节和验收场景由阶段设计与功能规格承接。代码不能反向定义本文件已经确定的业务语义。],
)

= 愿景、目标与边界

== 平台愿景

让学生在统一入口中获得可持续、可复现、受控且可追踪的异构算力实训环境；让教师和运营人员能够按班级安全地管理资源与教学过程；让平台能够从单节点试点平滑成长为多资源类型、多节点的教学基础设施。

== 长期业务目标

#data-table(
  ("目标", "长期结果", "稳定约束"),
  (
    ("学习连续性", "学生可从浏览器、SSH 或本地开发工具继续同一实训环境。", "数据和工作区语义不绑定具体节点。"),
    ("资源治理", "平台按班级、配置、配额和授权分配 CPU、NPU 及后续资源。", "用户不能直接指定特权运行条件。"),
    ("教学可见性", "教师获得经授权的学习过程、资源使用和异常证据。", "记录访问受班级隔离与最小化采集约束。"),
    ("安全隔离", "不同班级和用户之间的资源、数据、日志与权限相互隔离。", "默认拒绝且不泄露越权目标存在性。"),
    ("可持续演进", "新资源、新教学能力和新部署范围可逐步接入。", "已发布的状态、权限、审计和留存语义须兼容。"),
  ),
)

== 长期范围与非范围

平台长期覆盖身份与权限、班级治理、工作区与数据、异构算力资源、教学访问入口、过程记录、任务执行、审计、监控与运营治理。平台不以替代教务系统、通用云平台或任意业务服务托管平台为目标；这些能力如需集成，应使用明确的边界和独立规格。

= 总体架构

== 四层模型

#data-table(
  ("层", "长期职责", "边界规则"),
  (
    ("体验与访问层", "身份登录、门户、工作区入口、教师视图和受控任务提交。", "客户端提示不是权威授权；不得直连权威数据。"),
    ("教学控制面", "身份映射、RBAC、班级、配额、工作区操作、任务分配、审计与协调恢复。", "所有变更通过稳定业务操作追踪。"),
    ("资源运行面", "承载已验证的 CPU/NPU/后续异构工作负载并报告状态。", "只消费领域级运行规格，不接收原始浏览器请求。"),
    ("数据与证据面", "持久数据、终端记录、操作历史、审计、指标和归档。", "每项数据均带归属、范围、保留与访问策略。"),
  ),
)

== 依赖方向

业务规则位于领域与应用层；访问协议、数据库、运行时、消息系统和厂商设备能力属于适配层。传输层调用应用层，应用层编排领域规则，适配层实现端口。任何具体运行时或设备 SDK 均不得侵入领域模型。

== 控制面与运行面关系

控制面决定“谁可在何范围内使用何种批准资源、进行何种操作”；运行面执行“已被接受的工作区或任务规格”。运行结果必须回传为可关联的操作证据，控制面在超时、中断、重复投递或部分完成时通过协调流程收敛真实状态。

= 领域模型与归属

== 核心对象

#data-table(
  ("对象", "总设计语义", "归属关系"),
  (
    ("User", "具有稳定平台身份和状态的受控主体。", "平台"),
    ("Class", "成员、配置、配额、资源、日志和审计的最小教学治理范围。", "平台管理，成员在范围内活动"),
    ("Role / Permission / Binding", "可扩展的动作授权模型及其范围内绑定。", "角色不等于资源所有权"),
    ("Workspace Profile", "平台批准并由班级启用的资源选项。", "平台批准、班级启用"),
    ("Workspace / Volume", "用户持久实训环境及独立数据资源。", "一个班级、一个所有者"),
    ("Operation", "工作区或关键业务动作的请求、状态、结果、失败和重试关系。", "关联主体、范围和目标资源"),
    ("Task / Task Workspace", "受控长期任务及其独立运行、访问分配和数据边界。", "归属于班级，不复用学生个人工作区"),
    ("Terminal Record / Audit Record", "教学过程和安全行为的可查询证据。", "受班级范围和保留策略约束"),
  ),
)

== 不可变业务语义

* 外部身份映射必须稳定，电子邮件不能作为唯一用户键。
* 每个工作区必须归属一个班级与一个所有者；资源所有权不能替代权限判断。
* 生命周期操作必须有稳定标识、可查询状态和终态结果；同一请求的重复提交不能产生重复副作用。
* 停止和默认重置保留独立持久数据；删除必须明确表达保留或删除数据的选择与最终结果。
* 跨范围访问必须被拒绝，且响应不帮助调用者枚举目标资源。
* 重要管理、资源、访问拒绝和数据处置行为必须审计。

= 安全与多租户治理

== 授权模型

每次动作同时验证稳定身份、权限、班级范围、目标资源归属与状态。普通成员默认只操作自己的资源；教师、助教和管理员的跨用户动作必须来自显式权限绑定。角色、权限和绑定允许后续增删改查，但不能由此绕过范围检查或默认拒绝原则。

== 工作负载安全基线

工作负载只可使用平台批准的配置和运行能力，默认采用非特权、受资源限制的模式。禁止任意镜像、任意节点选择、高权限模式、宿主机网络、宿主机挂载和敏感凭证暴露。运行适配器不得接收浏览器 Cookie、用户令牌或未经验证的供应商配置。

== 证据与隐私

终端与教学记录遵循最小化采集、敏感内容过滤、范围内可见、访问可审计和按策略保留。日志与监控不得写入访问令牌、刷新令牌、Cookie、密码、私钥或完整敏感请求体。

= 资源与数据治理

== 资源类型演进

CPU、昇腾 NPU、GPU、其他 NPU 或专用设备都以“平台批准的工作区配置”接入。它们共享身份、班级、工作区、操作、持久化、审计和隔离语义；各类型可以拥有独立的容量、调度、并发、节点健康、队列和验收规则。

== 数据生命周期

数据资源独立于运行环境，具有班级、所有者、容量、绑定、保留、处置状态和配额影响。训练期结束、工作区删除、任务结束或成员变更都必须按已发布保留策略处理；任何自动清理均需有授权、可见结果和审计证据。

== 任务边界

长期任务由控制面提交和管理，拥有独立的任务工作区与访问分配。任务工作区不可作为绕过个人工作区隔离、班级配额或授权检查的通道；任务若要提供服务 API，必须作为新的平台能力单独规格化。

= 可靠性、可观测性与运营

#data-table(
  ("主题", "总设计要求"),
  (
    ("幂等", "同一业务请求重放返回同一操作结果；显式重试产生关联的新操作。"),
    ("并发", "冲突操作必须串行化或可协调；迟到结果不得覆盖较新代次。"),
    ("恢复", "运行面部分完成、超时、消息重复和进程中断后，先发现事实再收敛到明确状态。"),
    ("关联", "请求、后台处理、操作、运行结果、终端记录和审计以稳定关联标识串联。"),
    ("健康", "控制面、运行面、数据服务和采集链路均提供存活、就绪和可运营的状态。"),
    ("告警", "资源不足、节点异常、操作失败、采集缺口、配额耗尽和数据处置失败可被及时发现。"),
  ),
)

= 阶段路线图

#data-table(
  ("阶段", "目标", "主要交付", "不变量"),
  (
    ("一期：最小可用", "完成可试点的昇腾实训闭环。", "班级 RBAC、CPU/NPU 工作区、持久数据、三种入口、终端采集、任务工作区与基础运营。", "班级隔离、操作追踪、数据留存和审计。"),
    ("二期：教学优化", "提升教学编排和教师辅助效率。", "课程模板、批量操作、过程分析、协作查看、回放和自动判题。", "不得扩权或破坏一期证据语义。"),
    ("三期：资源扩展", "提升资源利用与部署范围。", "细粒度异构资源共享、多节点/多集群治理、构建流水线和更完整可观测性。", "资源类型扩展仍经批准配置和统一操作模型。"),
  ),
)

= 文档与变更治理

== 文档层次

#data-table(
  ("文档", "职责", "当前文件"),
  (
    ("总设计方案", "长期目标、架构不变量、领域边界和阶段治理。", "本文"),
    ("阶段总体方案", "某阶段的范围、路线图、风险和验收基线。", "`docs/design/spec1.typ`"),
    ("阶段详细设计", "某阶段的组件职责、流程、数据与验证细节。", "`docs/design/s1mvp1.typ`"),
    ("功能规格", "用户价值、验收条件、边界场景和非目标。", "`specs/<feature>/spec.md`"),
    ("计划、契约与任务", "实现设计、稳定接口、执行依赖与验证证据。", "`specs/<feature>/`"),
  ),
)

== 变更规则

影响状态含义、权限语义、数据留存、审计记录、外部契约或资源隔离的变化，必须先更新相应源文档，评估兼容性和迁移，再更新计划、契约、任务、实现和测试。实现便利性不能成为弱化安全或隔离规则的理由。

= 结论

本总设计确立了以班级隔离的持久工作区为平台核心、以控制面治理为安全边界、以操作与审计证据为可靠性基础的长期方向。后续每个阶段都应在这一基线上交付独立可验收的用户价值，并保留面向更广泛异构教学资源的扩展空间。
