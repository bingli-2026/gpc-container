#import "../template/project-kit.typ": *

#show: project-document.with(
  title: "昇腾算子远程实训平台设计方案",
  subtitle: "一期最小可用平台与后续演进路线",
  doc-type: "SYSTEM DESIGN REPORT",
  version: "V2.0",
  date: "2026-07-23",
  organization: "项目组",
  owner: "冶秉礼",
  status: "一期重构稿",
  toc: true,
)

= 执行摘要

本方案面向昇腾 C 算子远程实训。平台以班级为最小隔离和管理范围，为学生提供可持续使用的 CPU 或昇腾 NPU 工作区，并让教师能够在授权班级内掌握工作区状态、终端操作进度和异常原因。

一期目标不是构建完整教学平台，而是在 *15 个工作日* 内交付一个可供试点班级使用的闭环：身份登录、班级与成员管理、CPU/NPU 工作区生命周期、浏览器和 SSH 访问、持久数据、终端采集、班级隔离、基础监控与审计。后续课程模板、回放、判题、镜像构建和多集群能力均保留为二期演进项。

#metric-grid((
  metric-card("交付周期", "15 个工作日", detail: "一期最小可用闭环"),
  metric-card("实训对象", "CPU / 昇腾 NPU", detail: "统一工作区语义"),
  metric-card("隔离边界", "班级", detail: "成员、资源、日志、配额"),
  metric-card("试点规模", "约 30 人", detail: "单节点受控环境"),
))

#callout(
  "一期设计原则",
  [先保证“可登录、可创建、可访问、可留存、可追踪、可隔离”，再扩展教学编排能力。工作区状态、操作追踪、权限语义和数据保留选择一经发布，不得在后续阶段无迁移地改变。],
)

= 建设背景与目标

== 业务问题

昇腾算子实训同时需要编译环境、NPU 算力、代码留存和教学过程证据。直接在开发板上为每个学生维护账号，难以控制资源、无法隔离班级，也无法让教师获知学生的实际操作进度。平台应将这些能力收敛为受控的工作区服务。

== 一期目标

#data-table(
  ("目标", "一期承诺", "验收结果"),
  (
    ("实训入口", "浏览器 WebIDE、SSH 终端和 VS Code Remote-SSH 均可访问同一工作区。", "学生可任选入口继续已有实训。"),
    ("算力", "提供平台批准、班级启用的 CPU 与昇腾 NPU 工作区配置。", "不同类型均使用统一生命周期和操作追踪。"),
    ("隔离", "班级、成员、工作区、数据卷、日志和权限均按范围授权。", "跨班级及无所有权访问不泄露资源存在性。"),
    ("教学可见性", "采集 WebIDE 与 SSH 的终端输入、标准输出和错误输出。", "教师可在授权班级中查看进度和归档记录。"),
    ("持续性", "工作区停止或默认重置后保留用户持久数据。", "训练期内代码和数据不依赖某一块开发板。"),
  ),
)

== 非目标

一期不交付课程模板、教师远程控制或回放、自动判题、镜像构建流水线、安全扫描、vNPU/HAMi、多集群调度、学籍系统深度集成、数据快照、对象存储教学任务和公网服务发布。长期任务只提供控制面提交、查询和管理；任务工作区不得向平台内外提供可调用的服务 API。

= 一期范围与角色

== 班级是最小治理单元

班级承载成员关系、可用工作区配置、配额、资源归属、终端记录和审计边界。工作区所有者是一项资源关系，不等同于角色；每次访问同时校验调用者身份、权限及目标班级或资源范围。

== 初始角色与可扩展 RBAC

#data-table(
  ("角色", "一期职责", "范围"),
  (
    ("平台管理员", "维护平台规则、批准配置、节点可见性和全局审计。", "平台"),
    ("教师", "管理班级、成员、配置与班级内工作区；查看教学记录。", "被授权班级"),
    ("助教 / 操作人员", "按授予的权限处理班级资源并协助教学。", "被授权班级"),
    ("学生", "创建和管理自己的工作区，查看自己的操作和数据。", "本人资源及所属班级"),
    ("查看者", "只读查看被授予的班级资源。", "被授权班级"),
  ),
)

一期保留角色、权限和角色绑定的新增、查询、修改、停用/删除管理能力。默认角色仅是初始策略，后续可以在不改变资源所有权模型的情况下扩展权限组合。

= 总体架构

== 逻辑分层

#data-table(
  ("层次", "职责", "一期组成"),
  (
    ("访问层", "统一身份登录、门户、工作区入口与教师查看。", "Web 门户、WebIDE、SSH、VS Code Remote-SSH"),
    ("控制面", "身份映射、RBAC、班级治理、工作区与操作编排、审计。", "控制服务、操作队列、关系数据与缓存"),
    ("运行面", "承载 CPU/NPU 工作区与长期任务工作区，执行生命周期。", "单节点受控集群、Orange Pi AIPro 节点、设备共享能力"),
    ("数据与证据", "训练期数据、终端记录、指标和归档。", "共享持久存储、日志归档、监控与告警"),
  ),
)

控制服务运行在 x86 控制节点；Orange Pi AIPro 8T/20T 节点承载昇腾实训工作区。节点型号可以混合接入，但调度、配额和验收必须分别反映 CPU 与 NPU 的可用资源。运行面只接收已经验证的领域级工作区规格，不能接收浏览器请求或任意运行参数。

== 关键业务对象

#data-table(
  ("对象", "业务含义"),
  (
    ("用户与班级成员", "外部稳定身份映射为唯一平台用户；成员关系确定其班级职责和有效范围。"),
    ("工作区配置", "平台批准、班级启用的 CPU 或 NPU 选项；学生不得自行指定运行环境、资源量或特权。"),
    ("工作区与数据卷", "工作区归属于一个班级和一个所有者；数据卷与运行环境分离，记录容量、绑定、保留和配额影响。"),
    ("工作区操作", "创建、启动、停止、重置、删除等生命周期请求的稳定追踪证据，包含结果、失败及重试关联。"),
    ("终端操作记录", "按班级、用户、工作区、时间和流向关联的输入/输出证据，经过敏感信息过滤。"),
    ("长期任务与任务工作区", "经控制面 API 提交的批处理任务；使用独立、可分配且有班级边界的任务工作区。"),
  ),
)

= 核心设计决策

== 工作区与算力

CPU 与 NPU 工作区共享创建、状态、持久数据、删除选择、审计和操作追踪语义。NPU 额外遵守节点可用性、设备共享、每节点并发工作区上限及编译并发排队规则；一期目标是在受控条件下支撑每个可用 NPU 节点至少 3 个 NPU 工作区并行运行。资源不足、配置撤销或节点不可用时，操作进入明确失败或等待终态，不得隐式降级为未经批准的配置。

== 生命周期、幂等与恢复

每个生命周期请求生成唯一操作标识，并处于等待、执行、成功或失败等可查询状态。同一调用者对同一业务请求重复提交，返回同一操作结果，不重复创建资源；对可恢复失败的显式重试创建新的、关联原操作的记录。较旧操作的迟到结果不得覆盖较新的工作区代次。

若外部运行环境已部分完成而控制进程中断，系统必须先识别和协调实际状态，再决定完成、失败或补偿，不能盲目再次创建。成员资格、配额或配置在操作期间变化时，应按已接受、未执行和已产生外部副作用分别处理，留下审计证据，并最终避免孤儿资源。

== 数据留存与训练期结束

停止和默认重置均保留独立用户持久数据。删除时，用户必须明确选择“保留数据”或“同时删除数据”，并能看到最终处理结果。训练期结束时，默认停止学生工作区、保留数据并限制学生继续访问；教师和平台管理员可按授权批量导出或启动清理。所有保留数据仍有明确的班级、所有者、配额影响和后续处置状态。

== 终端采集与教学边界

平台采集 WebIDE 和 SSH（包括 VS Code Remote-SSH）会话的终端输入、标准输出和错误输出，提供实时查看与训练期归档。记录按班级边界授权，教师/助教不能浏览其他班级；密码、令牌、私钥和其他敏感片段必须过滤，用户可见错误也不得暴露运行时凭证、堆栈或数据库细节。

== 长期任务

长期任务由有权限的调用者通过控制面提交，平台为其创建或分配独立任务工作区。任务工作区必须与学生个人工作区隔离，访问分配只在同一班级且经显式授权的主体之间生效，并记录审计。任务本身不得对外或对平台内部暴露业务服务 API；服务化能力属于后续独立需求。

= 安全、运维与证据

- 所有访问默认拒绝，并同时检查稳定身份、角色权限、班级范围和资源所有权；跨班级或猜测资源标识的请求使用不泄露存在性的响应。
- 工作区使用非特权、受限运行能力；不允许任意镜像、任意资源请求、宿主机网络、宿主机挂载或高权限模式。
- 创建、删除、重置、成员和 RBAC 变更、跨用户操作、管理操作及访问拒绝都必须产生可查询审计证据。
- 用户请求、后台处理、操作记录、终端记录和审计记录以稳定关联信息串联；日志不得记录令牌、Cookie、密码或私钥。
- 服务提供存活与就绪信息；监控覆盖节点、CPU/NPU 资源、工作区状态、操作积压、终端采集和存储使用情况。

= 一期交付计划

15 天是并行工作流的总体日历，而不是把所有事项串行相加。每项完成以可演示和可验收为准。

#data-table(
  ("时间", "交付流", "可验证结果"),
  (
    ("第 1--4 天", "环境与基础身份", "控制节点、受控运行节点、统一登录、基础监控和存储可用；8T/20T 节点可被识别。"),
    ("第 3--9 天", "班级、RBAC 与工作区", "班级成员、可扩展角色绑定、CPU/NPU 配置、创建/启停/重置/删除及操作追踪贯通。"),
    ("第 6--12 天", "访问与教学证据", "WebIDE、SSH、VS Code Remote-SSH 可达；终端采集、过滤、实时查看和归档可用。"),
    ("第 10--13 天", "长期任务与恢复", "控制面任务提交、独立任务工作区、分配隔离、超时/重复/中断协调覆盖。"),
    ("第 13--15 天", "验收与交接", "多班级隔离、持久化、NPU 并发、审计、故障恢复和运维手册完成验证。"),
  ),
)

= 一期验收标准

#checklist((
  (true, [在镜像可用且资源充足的受控单节点环境，连续至少 20 次创建 CPU 工作区，至少 19 次在 3 分钟内到达可用或明确失败终态。]),
  (true, [CPU、NPU 工作区均只能选择平台批准且班级已启用的配置；每个可用 NPU 节点至少可并行运行 3 个受控 NPU 工作区。]),
  (true, [所有停止后启动和默认重置场景保留用户持久数据；删除后的最终数据状态与用户选择一致。]),
  (true, [双班级隔离测试中，全部跨班级、无所有权和资源枚举访问被拒绝，且不泄露目标存在性。]),
  (true, [WebIDE、SSH 和 VS Code Remote-SSH 均能进入同一工作区；对应终端输入、输出和错误输出可在授权班级内检索，并完成敏感信息过滤。]),
  (true, [重复提交、后台消息重复处理、并发操作和迟到结果不产生重复资源或覆盖新状态。]),
  (true, [创建、删除、重置、成员/RBAC 变更、跨用户操作、访问拒绝及长期任务分配均有可查询审计证据。]),
  (true, [预定义可恢复故障下，系统经自动重试、显式重试或协调流程到达明确终态，不产生重复资源或持久数据丢失。]),
))

= 二期及以后路线图

#data-table(
  ("方向", "后续能力"),
  (
    ("教学编排", "课程模板、环境预置、任务单、教学资料和批量班级操作。"),
    ("教学辅助", "教师远程协助、会话回放、学习过程分析与自动判题。"),
    ("工程链路", "镜像构建、质量与安全扫描、可信发布。"),
    ("资源演进", "更细粒度 NPU 共享、更多节点、跨集群调度与容量治理。"),
    ("平台集成", "学籍/教务集成、对象存储、数据快照和服务发布等独立能力。"),
  ),
)

= 主要风险与治理

#data-table(
  ("风险", "影响", "一期控制"),
  (
    ("NPU 编译争用", "交互体验下降或任务堆积。", "限制每节点并发 NPU 工作区，编译任务排队并展示操作进度。"),
    ("外部状态不确定", "超时或中断后可能重复创建。", "操作标识、幂等、代次保护和协调恢复。"),
    ("终端采集泄密", "教学证据中出现敏感信息。", "采集前过滤、最小授权、归档访问审计和保留策略。"),
    ("15 天范围膨胀", "交付无法形成闭环。", "严格按一期非目标执行，新增能力进入二期路线图。"),
  ),
)

= 结论

一期以班级隔离的 CPU/NPU 持久工作区为基础，补齐教学实训真正需要的访问入口、终端过程证据、资源治理和可恢复操作。通过固定这些稳定语义，平台可以在不破坏既有用户数据与权限模型的前提下，逐步扩展为完整的昇腾算子远程实训平台。
